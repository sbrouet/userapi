package com.sbr.userapi;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class UserApiApplicationTest {

	/**
	 * Launch a test that causes the Spring context to be loaded and initialize the
	 * datasource, JPA, Hibernate and all other configured Spring beans/components.
	 * This allows to validate that the application is configured correctly in the
	 * test application context and is able to run
	 */
	@Test
	void contextLoads() {
	}

}
